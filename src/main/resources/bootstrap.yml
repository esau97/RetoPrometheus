spring:
  application:
    name: Celsius-Fahrenheit
  cloud:
    config:
      enabled: true
      uri: http://localhost:8888
management:
  endpoints:
    web:
      exposure:        include:         - "*"
      base-path: /actuator